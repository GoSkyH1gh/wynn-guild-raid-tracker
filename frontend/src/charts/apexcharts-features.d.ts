// The apexcharts `features/*` subpaths ship no type declarations — they only
// have side effects (registering chart types/features onto the shared core
// class), so an ambient declaration is enough.
declare module "apexcharts/features/legend";
declare module "apexcharts/features/toolbar";
