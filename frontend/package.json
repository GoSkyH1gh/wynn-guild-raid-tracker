{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:picker": "esbuild test-cycle-picker.ts --bundle --platform=node --format=cjs --outfile=node_modules/.cache/test-cycle-picker.cjs && node node_modules/.cache/test-cycle-picker.cjs",
    "test:series": "esbuild test-series.ts --bundle --platform=node --format=cjs --outfile=node_modules/.cache/test-series.cjs && node node_modules/.cache/test-series.cjs",
    "test:dom": "esbuild test-analytics-dom.ts --bundle --platform=node --format=cjs --outfile=node_modules/.cache/test-analytics-dom.cjs && node node_modules/.cache/test-analytics-dom.cjs",
    "test": "npm run test:picker && npm run test:series && npm run test:dom"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.1",
    "happy-dom": "^20.11.1",
    "typescript": "^5.7.0",
    "vite": "^6.3.0"
  },
  "dependencies": {
    "apexcharts": "^6.10.0"
  }
}
